#include "main.h"

/**
 * _strlen - calculates the length of the string
 * @s: string
 *
 * Return: chain length
 */

int _strlen(char *s)
{
	int i;

	for (i = 0; s[i] != '\0'; i++)
	{
		;
	}

	return (i);
}
